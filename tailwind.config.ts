import { type Config } from 'tailwindcss'

// Tailwind v4 reads theme tokens from CSS (@theme/@custom-variant).
// Keep the config empty so v3-style theme overrides do not interfere.
const config: Config = {}

export default config
